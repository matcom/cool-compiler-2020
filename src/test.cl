class Main inherits IO
{
   s : String;
   a : Int <- 2;
   main():Object
   {
      {
         s <- in_string();
         out_int(s.length());
                  s <- in_string();
         out_int(s.length());
      }
   };

};